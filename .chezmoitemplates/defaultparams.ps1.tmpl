# chezmoi cred admin {{- $credAdmSbk := "{ Get-Secret -Name da -Vault dp }" -}}
# chezmoi cred ssp {{- $credSspAdmSbk := "{ if ($args.BoundParameters -contains 'Authentication') { Get-Secret -Name da -Vault dp } }" }}
$PSDefaultParameterValues = @{
  'Get-ADUserProxy:Properties' = [string[]]@(
    'Description'
    'DisplayName'
    'LockedOut'
    'PasswordExpired'
    'Office' # physicalDeliveryOfficeName
    'Surname'
    'Title'
    'msDS-AllowedToDelegateTo'
    'whenCreated'
    'whenChanged'
    # chezmoi if company {{- if ne .workOneDriveBusinessName "" }}
    # chezmoi get company Property {{- $companyProp := list "extensionAttribute2" "extensionAttribute4" "extensionAttribute11" "extensionAttribute12" "EmployeeID" }}
    # chezmoi range {{ range $currentCompProp := $companyProp }}{{ $currentCompProp | squote | nindent 4 }}{{ end }}
    # chezmoi end if company {{- end }}
  )
  'Get-ADGroupProxy:Properties' = [string[]]@(
    'Description'
    'DisplayName'
    'whenCreated'
    'whenChanged'
  )
  'Search-Giphy:ApiKey' = { Get-Secret -Name giphy -Vault dp }
  'Invoke-DbaWhoIsActive:FormatOutput' = 2 # Not perfect. This does line up the output better. Using 0 removes the default first column for time spent.
  'Invoke-Mstsc:Credential' = {{ $credAdmSbk }}
  'Invoke-Command:Credential' = {{ $credSspAdmSbk }}
  'Enter-PSSession:Credential' = {{ $credSspAdmSbk }}
  'New-PSSession:Credential' = {{ $credSspAdmSbk }}
  'Out-Default:OutVariable' = '__' # "All" output is written to $__ to make it easier to work interactively like if a user forgets to assign the output.
  'Format-Table:AutoSize' = $true
  'Format-Table:Wrap' = $true
  # chezmoi if mgmt sql {{- if ne .msSqlMgmtInstance "" }}
  'Get-DbaRegServer:SqlInstance' = {{ .msSqlMgmtInstance | squote }}
  # chezmoi end {{- end }}
  # chezmoi if vCenter {{- if ne .vCenterComputerName "" }}
  'Connect-VIServer:Server' = {{ .vCenterComputerName | squote }} 
  # chezmoi end {{- end }}
  # chezmoi if mgmt sql {{- if ne .workKeepassName "" }}
  'Unlock-SecretVault:Name' = {{ .workKeepassName | squote }}
  'Unlock-SecretVault:Password' = { Get-Secret -Name dbakey -Vault dp }
  # chezmoi end {{- end }}
}
